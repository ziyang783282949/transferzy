<?xml version="1.0" encoding="UTF-8"?><!--Converted at: Mon Aug 20 17:02:07 
	CST 2012 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BlindModel">

	<resultMap id="BaseResultMap" type="BlindModel">
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="name" jdbcType="VARCHAR" property="name" />
		<result column="blind_list" jdbcType="TIMESTAMP" property="blindList" />
		<result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
		
	</resultMap>

	<insert id="addEntity" parameterType="java.util.Map">
		insert into 
		tb_blind(
			name,blind_list,create_time
		)values(
			#{name},#{blindList},#{createTime}
		)
		
	</insert>

	<select id="findEntityListByCond" parameterType="java.util.Map" resultMap="BaseResultMap">
	
		select * from tb_blind
		<trim prefix="where" prefixOverrides="and" >
			<if test="id != null">
				and id=#{id} 
			</if>
			<if test="name != null">
				and name=#{name}
			</if>
			
		</trim>
		
	</select>

	<update id="updateEntity" parameterType="java.util.Map">
		update tb_blind
		<trim suffixOverrides = ",">
		<set>
			<if test="name != null">
				name=#{name},
			</if>
			<if test="eventStatus != null">
				event_status = #{eventStatus},
			</if>
			<if test="basePot != null">
				basePot = #{basePot},
			</if>
			<if test="rebuyTimes != null">
				rebuy_times = #{rebuyTimes},
			</if>
			<if test="curPlayers != null">
				cur_players = #{curPlayers},
			</if>
			<if test="rewardInfo != null">
				reward_info = #{rewardInfo},
			</if>
		</set>
		</trim>
		where id = #{id}
	</update>
	
</mapper>